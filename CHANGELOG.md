# Changelog

All notable changes to this project are documented here automatically by
`nx release` (via `.github/workflows/release.yml`), parsing Conventional
Commit messages on every merge to `main`.

## [Unreleased]
- Initial showcase platform: trade-capture-service, pnl-risk-consumer, Angular frontend, Nx monorepo, CI/CD.
